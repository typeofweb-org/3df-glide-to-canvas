{
  "name": "3df",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "deploy": "yarn build && mkdir -p gh-pages && cp -R {dist,index.html,examples} ./gh-pages && gh-pages -d gh-pages && rm -rf gh-pages"
  },
  "devDependencies": {
    "typescript": "4.1.3"
  }
}
